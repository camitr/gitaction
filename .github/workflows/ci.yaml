name: test pipeline
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    name: Deploy to kubernetes
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up QEMU 🏗️
        uses: docker/setup-qemu-action@v1
      - name: Set up Docker Buildx 🐋
        uses: docker/setup-buildx-action@v1
      - name: Login to DockerHub Registry
        run: docker login -u amit.shrivastava@forcepoint.com -p AKCp8jQwmF6d9R89MBYUZhGxANtu1Vr6c5gya8tzyAUUVGai5tgFXmpbt9LdFYuRx5Tw7nNVd rbi-docker.us-artifactory.cicd.cloud.fpdev.io
      - name: Build and push 🐳
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: rbi-docker.us-artifactory.cicd.cloud.fpdev.io/githubaction-test:latest